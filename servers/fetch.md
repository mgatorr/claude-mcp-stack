# fetch

**Category:** web · **Runtime:** uvx · **Package:** `mcp-server-fetch`

Fetch a URL and return its content converted to Markdown for the model to read. Useful for
pulling in articles, docs, and pages during a conversation.

## API key

None required.
